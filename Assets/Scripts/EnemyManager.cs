using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyManager : MonoBehaviour
{
    private Enemy enemy;


    public List<EnemySpawnPoint> list_of_enemies = new List<EnemySpawnPoint>();


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void SpawnEnemies(GameObject spawn_point, int number_of_enemies, float spawn_timer)
    {

    }
}
